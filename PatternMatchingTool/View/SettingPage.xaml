<Page x:Class="PatternMatchingTool.View.SettingPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:PatternMatchingTool.View"
        xmlns:view="clr-namespace:PatternMatchingTool.View"
        mc:Ignorable="d"
        Title="SettingPage" MaxHeight="920">
    <Grid>
        <ui:NavigationView PaneDisplayMode="Top"
                           x:Name="SettingNavigationView"
                           IsBackButtonVisible="Collapsed" 
                           IsPaneToggleVisible="False"
                           IsPaneVisible="True"
                           Width="Auto"
                           UseLayoutRounding="True"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           ScrollViewer.VerticalScrollBarVisibility="Disabled"
                           ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                           Background="Red">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="CAMERA" 
                                       Icon="Auto" 
                                       Tag="AutoPage" 
                                       Cursor="Hand" 
                                       NavigationCacheMode="Enabled"
                                       TargetPageType="{x:Type view:SettingCameraPage}"/>
                <ui:NavigationViewItem Content="PATTERN" 
                                       Icon="Settings" 
                                       Tag="SettingsPage" 
                                       NavigationCacheMode="Enabled"
                                       TargetPageType="{x:Type view:SettingPatternPage}"/>
            </ui:NavigationView.MenuItems>
        </ui:NavigationView>
    </Grid>
</Page>
